class UserDefinedOptions:

    ## Model organism
    species = input('Enter the species code (Options: hs, mm, dm, ce, dr or custom): ')
    bt_parameter = input('Bowtie run parameters (default runs in the default mode): ')
